name: Qwen two-token prefix metrics macOS

on:
  push:
    branches: [feat/qwen-prompt-cache-metrics]

permissions:
  contents: write

jobs:
  fixture:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/qwen-prompt-cache-metrics
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: c/tools/requirements-deepseek-v4-tiny.txt
      - run: python -m pip install -r c/tools/requirements-deepseek-v4-tiny.txt
      - run: python c/tools/make_qwen_moe_tiny.py --output c/qwen_moe_tiny --force
      - uses: actions/upload-artifact@v4
        with:
          name: qwen-prefix-two-token-tiny
          path: c/qwen_moe_tiny
          if-no-files-found: error

  macos:
    needs: fixture
    runs-on: macos-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/qwen-prompt-cache-metrics
          fetch-depth: 0
      - run: brew install libomp
      - uses: actions/download-artifact@v4
        with:
          name: qwen-prefix-two-token-tiny
          path: c/qwen_moe_tiny
      - name: Build native engine and require non-vacuous hybrid oracle
        run: |
          make -C c qwen_moe ARCH=native
          python3 c/tests/test_qwen_prefix_serve.py --binary c/qwen_moe --fixture c/qwen_moe_tiny
      - name: Require two-token fixed-memory metrics on Apple Silicon
        run: |
          printf '<t005><t007><t009><t011><t013><t017><t019><t023>' > /tmp/qwen-prefix.txt
          printf '<t029><t031><t037><t041><t043><t047>' > /tmp/qwen-tail.txt
          python3 c/tools/benchmark_qwen_prefix_cache.py \
            --binary c/qwen_moe --model c/qwen_moe_tiny \
            --prefix-file /tmp/qwen-prefix.txt --tail-file /tmp/qwen-tail.txt \
            --memory-gb 4.5 --cache-mb 8 --min-prefix-tokens 1 \
            --context 128 --max-tokens 2 --trials 1 \
            --output /tmp/qwen-two-token-macos.json
          python3 - <<'PY'
          import json, platform
          p = json.load(open('/tmp/qwen-two-token-macos.json'))
          s = p['summary']
          assert p['matched_tokens'] == 8
          assert p['pairs'][0]['cache_on']['measure']['data_frames'] >= 2
          assert p['pairs'][0]['cache_off']['measure']['data_frames'] >= 2
          assert 'first_to_second_penalty_median_sec' in s
          assert 'completion_delta_median_sec' in s
          print('machine=', platform.machine())
          print('summary=', json.dumps(s, sort_keys=True))
          PY
      - name: Remove temporary workflow
        run: |
          git rm .github/workflows/qwen-prefix-two-token-macos.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore(qwen): remove temporary two-token macOS gate"
          git push origin HEAD:feat/qwen-prompt-cache-metrics
